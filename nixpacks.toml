[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd react-frontend && npm install"
]

[phases.build]
cmds = ["cd react-frontend && npm run build"]

[start]
cmd = "python -m uvicorn app:app --host 0.0.0.0 --port $PORT"
